version: "3.8"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8080:8080"

  frontend:
    build: ./my-app
    container_name: frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend