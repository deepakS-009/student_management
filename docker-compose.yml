version: '3.8'

services:

  mysql:
    image: mysql:8.0
    restart: always

    environment:
      MYSQL_ROOT_PASSWORD: Deepak@003
      MYSQL_DATABASE: student_db

    ports:
      - "3307:3306"

    volumes:
      - mysql-data:/var/lib/mysql

  backend:
    build: ./backend

    depends_on:
      - mysql

    ports:
      - "8080:8080"

    restart: always

  frontend:
    build: ./my-app

    depends_on:
      - backend

    ports:
      - "5173:5173"

    restart: always

volumes:
  mysql-data: